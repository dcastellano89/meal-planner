# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a meal-planner application project currently in initial setup phase.

## MCP Integration

**Context7 MCP Usage**: Always use Context7 MCP when I need library/API documentation, code generation, setup or configuration steps without me having to explicitly ask.

## Supabase Configuration

This project uses Supabase as the backend. The Supabase MCP server is configured in `.mcp.json` with project reference: `cxrckwyodmqpoznoubmx`.
